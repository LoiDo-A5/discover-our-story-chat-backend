version: '3'

services:
  web:
    build: .
    container_name: chatroom-web
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
